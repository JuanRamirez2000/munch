import { ScreenCenter } from "@/components/ScreenContainer";

// Placeholder — the real results/leaderboard screen lands in Phase 5.
export default function ResultsPlaceholderPage() {
  return (
    <ScreenCenter>
      <div className="text-[19px] font-bold text-ink">Session finished</div>
      <div className="text-[14.5px] text-ink-muted">The results screen is coming in Phase 5.</div>
    </ScreenCenter>
  );
}
